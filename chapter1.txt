It was the best of times, it was the worst of times.

git init -- to initalize the repository
git status will check which file is saved to repo 
git log will show all the changes when they were made
git commit -m "" will add the changes to add the files to a repo git add .
to check the difference from the existing one till the changes made can be done by git diff file.ext
to go back to the previous version of the file: git checkout file.ext
git remote add origin https://github.com/psakshi-989/Story.git to add local repo
git push -u origin master to add the files in github repo
git clone ./url will download the files of that project on the github
